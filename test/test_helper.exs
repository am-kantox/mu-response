ExUnit.start

# Mix.Task.run "ecto.create", ["--quiet"]
# Mix.Task.run "ecto.migrate", ["--quiet"]

# Ecto.Adapters.SQL.Sandbox.mode(Repo, :manual)
# Ecto.Adapters.SQL.begin_test_transaction(MuResponse.Repo)
